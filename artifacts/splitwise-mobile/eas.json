{
  "cli": {
    "version": ">= 12.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "prebuildCommand": "cd ../.. && npm install -g pnpm@latest --silent && pnpm install --no-frozen-lockfile",
      "ios": {
        "simulator": false
      },
      "android": {
        "buildType": "apk"
      }
    },
    "preview": {
      "distribution": "internal",
      "prebuildCommand": "cd ../.. && npm install -g pnpm@latest --silent && pnpm install --no-frozen-lockfile",
      "ios": {
        "simulator": false
      },
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "autoIncrement": true,
      "prebuildCommand": "cd ../.. && npm install -g pnpm@latest --silent && pnpm install --no-frozen-lockfile"
    }
  },
  "submit": {
    "production": {}
  }
}
